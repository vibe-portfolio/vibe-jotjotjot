# Vercel KV (Get these from Vercel Dashboard > Storage > KV)
KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=

# Base URL for your app (use your production URL when deployed)
NEXT_PUBLIC_BASE_URL=http://localhost:3000
